# ANCHOR_STATE_DIR=.anchor
# ANCHOR_USER_DIR=/Users/you/.anchor
# A leading "~" (e.g. ~/.anchor) is also expanded, but MCP client configs
# are JSON/TOML env blocks with no shell to do that for you — prefer an
# absolute path if you're not sure your client expands it.
